# Memory

[2026-05-23] (Mem-0001) Created initial planning notes for a modern local Spark Compose platform based on concepts from `master-spark-plumbers` and `spark-frm-mec`: Spark standalone, persisted event logs, History Server, REST API, `spark-submit`, and later Spark Connect for agent workflows.
[2026-05-23] (Mem-0002) Validated the early compatibility approach with official image `apache/spark:4.1.2-scala2.13-java17-python3-ubuntu`, Spark 4.1.2, History Server, SQL smoke tests, and Delta 4.2.0 via `io.delta:delta-spark_4.1_2.13:4.2.0`; v0 stays with classic `spark-submit` first.
[2026-05-24] (Mem-0003) Planned and scaffolded final v0 layout: Make-based bootstrap/build/compose/smoke/log-ingestion flow, local Spark jar cache, MinIO `lakehouse` and `spark-logs` buckets, Go event-log loader, and ClickHouse observability tables.
[2026-05-24] (Mem-0004) Updated final v0 image architecture: Compose now targets project-local images for Spark runtime, Spark History, MinIO server, MinIO client, ClickHouse, and the Go event-log loader; Spark requirements moved into `build/images/spark/requirements.txt`; MinIO bucket initialization moved from inline Compose YAML into `build/images/minio/init-buckets.sh`.
[2026-05-24] (Mem-0005) Changed Docker builds to per-image contexts (`build/images/<image>`). Spark receives a generated image-local context under `build/images/spark/context`, so no final image build traverses or depends on exploratory lab files.
[2026-05-24] (Mem-0006) E2E validation passed: bootstrap reused local jars/wheels/vendor after base pulls; build created six local images; compose started Spark, History, MinIO, and ClickHouse; smoke wrote Delta to MinIO; Spark History indexed two apps; Go loader ingested event logs into ClickHouse with final counts raw=866, sql=19, stages=22, tasks=321.
[2026-05-24] (Mem-0007) Added `docs/logs-info` documentation for ClickStack usage, current ClickHouse tables, Spark event-log coverage, physical-plan semantics, and next normalized table candidates; clarified that the Go loader stores raw event-log JSON broadly but only normalizes SQL start/end, stage completed, and task end events today.
[2026-05-24] (Mem-0008) Added project-level next steps and `agent_readme.md`; documented the next loader direction as a behavior-preserving Go refactor into entity/responsibility packages, with Spark Connect kept as a later control-plane experiment for agent workflows.
[2026-05-24] (Mem-0009) Added uv-managed Python project scaffolding, config-driven lakehouse IO utilities, fake Spark unit tests for read/write behavior without a cluster, and a customer bronze smoke flow targeting `s3a://lakehouse/bronze/customer`; MinIO init now creates bronze/silver/gold customer prefixes.
[2026-05-24] (Mem-0010) Moved read/write spec validation out of `io` into `spark_platform.factory`, keeping `io` focused on Spark reader/writer execution and preserving fake Spark unit-test coverage.
[2026-05-24] (Mem-0011) Moved Spark session creation from generic `core` into `spark_platform.session`, keeping `factory` for IO specs and `io` for Spark reader/writer execution.
[2026-05-24] (Mem-0012) Moved job-specific Spark app names out of lakehouse YAML; runner scripts now pass app names explicitly to `SparkSessionFactory`, which keeps `DEFAULT_APP_NAME` only as a fallback.
[2026-05-24] (Mem-0013) Updated the customer bronze smoke script with a formation-style header, manual spark-submit command, explicit execution sequence, and logger-based plan/result logging instead of DataFrame.explain stdout output.
[2026-05-24] (Mem-0014) Removed direct physical-plan logging from the smoke flow; kept the DataFrame formatted-plan helper commented as an optional debugging reference because ClickHouse receives SQL plans through Spark event logs.
[2026-05-24] (Mem-0015) Moved the commented formatted-plan debugging reference out of the smoke job into `spark_platform.utils.plan_debug`, keeping smoke focused on execution while documenting that physical plans should normally come from Spark event logs loaded into ClickHouse.
[2026-05-24] (Mem-0016) Documented the smoke script `__main__`/`SystemExit(main())` pattern and updated bootstrap to ensure `uv` is available, installing it via the official Astral installer when missing, then running `uv sync`.
[2026-05-24] (Mem-0017) Added `make tests` as the primary Python unit-test target and kept `make test` as an alias for compatibility.
[2026-05-24] (Mem-0018) Introduced ABC-based dataset reader/writer strategies by format; public `read_dataset` and `write_dataset` now validate specs and delegate to reader/writer factories, preparing IO for future formats without changing smoke scripts.
[2026-05-24] (Mem-0019) Removed `collect()` from the smoke job; SQL aggregation is materialized with a scalar count so Spark does the grouped work before returning only a small driver-side metric.
[2026-05-24] (Mem-0020) Simplified Spark app structure: removed reader/writer strategy factories, moved specs to `io/specs.py`, unified dataset operations in `io/datasets.py`, added `SparkPlatJob` ABC/template in `jobs/base.py`, and converted the smoke app to the job contract with `src/apps/template.md`.
[2026-05-24] (Mem-0021) Made `SparkSessionFactory` import PySpark lazily inside session lifecycle methods so Python unit tests for the job contract can run without a local PySpark installation.
[2026-05-24] (Mem-0022) Updated the customer bronze smoke app to use DataFrame aggregation instead of SQL strings, removed the unnecessary `ORDER BY`, and changed app/template manual submit docs to assume Compose is already running via `docker exec` against `spv0-spark-master`.
[2026-05-24] (Mem-0023) Updated the smoke transformation to use PySpark `DataFrame.transform(...)` with a named helper and expanded the app template with transform-vs-direct examples plus explicit guidance to avoid unnecessary `orderBy()`.
[2026-05-24] (Mem-0024) Restructured sample app flow under `src/apps/sample_scripts`: landing ingestion writes customer JSON, the contract-based bronze job reads landing and writes Delta, and a separate sanity script validates landing/bronze outputs; `make smoke` now runs the full sequence.
[2026-05-24] (Mem-0025) Inlined the customer sample payload into the landing ingestion script and added `src/apps/sample_scripts/README.md`; sample scripts now carry explicit step docstrings for script-level onboarding.
[2026-05-24] (Mem-0026) Updated `make clean-data` with a Docker root fallback for root-owned bind-mounted files, using the project Spark image when available and the bootstrapped Spark base image otherwise.
[2026-05-24] (Mem-0027) Removed the obsolete compatibility lab and moved historical brainstorm notes to `docs/old-brainstorm` so final project code no longer carries root-level brainstorm artifacts.
[2026-05-24] (Mem-0028) Documented the local bind-mount permission risk after E2E cleanup hit root-owned MinIO and ClickHouse files; operations docs now explain the `make clean-data` Docker-root fallback and recommended Spark user follow-up.
[2026-05-24] (Mem-0029) Updated Spark runtime and Spark History images to return to the upstream `spark` user after root-only build steps, reducing local root-owned Spark artifacts while keeping stateful MinIO/ClickHouse cleanup fallback.
[2026-05-24] (Mem-0030) Added `docs/assets/spark-platform-v0-visual-prompt.md` for generating the README visual asset, updated README with the future image link only, and refreshed `agent_readme.md` for the current no-compat-lab project state.
[2026-05-24] (Mem-0032) Added `docs/machine-requirements.md`, linked it from README, and documented `make test` as the alias for `make tests` in agent/operations docs.
[2026-05-24] (Mem-0033) Shortened `docs/machine-requirements.md`, added pinned runtime versions, and removed repeated verification commands.
[2026-05-24] (Mem-0034) Refocused `docs/machine-requirements.md` on host CLI versions and install instructions only, removing runtime/ports/cleanup content from that page.
[2026-05-24] (Mem-0035) Moved architecture, compatibility, and operations docs under `docs/dev-design` and added a feature-oriented Spark design discussion under the dev-design docs.
[2026-05-24] (Mem-0036) Reworked the Spark patterns doc into `docs/dev-design/design-pattern-v0-disccusion.md`, focusing on the rationale and valuation of lightweight guardrails for the v0 Spark optimization platform.
[2026-05-24] (Mem-0037) Refocused `docs/dev-design/design-pattern-v0-disccusion.md` around the agentic Spark code optimization goal, emphasizing lightweight guardrails to reduce agent drift without making architecture the product.
[2026-05-24] (Mem-0038) Updated the design-pattern discussion with agentic coding references, clarified that the sanity-check split is basic Spark hygiene, and marked the valuation sections as Codex GPT-5-based opinion.
[2026-05-24] (Mem-0039) Reworked external references in the design-pattern discussion toward software engineering practices and repository-level agent guidance that reduce coding drift.
